# --- E2E test constants ---
# Mirrors constants from core/constants/src/lib.rs
# If the Rust constants change, update these accordingly.

SIGNERS_DIR="asfaload.signers"
PENDING_SIGNERS_DIR="asfaload.signers.pending"
SIGNERS_FILE="index.json"
INDEX_FILE="asfaload.index.json"
SIGNATURES_SUFFIX="signatures.json"
PENDING_SIGNATURES_SUFFIX="signatures.json.pending"
SIGNERS_SUFFIX="signers.json"
REVOCATION_SUFFIX="revocation.json"
REVOKED_SUFFIX="revoked"
SIGNERS_HISTORY_FILE="asfaload.signers.history.json"


backend=http://localhost:3000
key_password="password"
