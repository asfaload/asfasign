#!/usr/bin/env bash
set -euo pipefail

# Driver for `make demos`. Builds binaries, runs hidden infrastructure,
# and renders client-cli/docs/demos/howto/*.tape into GIFs.
# Real implementation lands incrementally in subsequent tasks.

echo "run-demos.sh: not yet implemented" >&2
exit 1
